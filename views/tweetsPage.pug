script
    include ../public/js/clickHandlers.js

ol
    each tweet, i in tweets
        li.tweet
            span.text #{tweet.full_text}
            span.time @#{tweet.created_at}

        if i == tweets.length - 1
            a(onclick=`handleOnClickLoadMore(${nextPageUrl})`) LOAD MORE

